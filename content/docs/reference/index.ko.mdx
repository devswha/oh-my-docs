---
title: 레퍼런스
description: CLI, config, category, install flag, 환경 변수 빠른 표
---

## 패키지와 이름

rename은 아직 전환 중입니다. 제품 이름은 OpenAgent지만, 설치 명령은 upstream이 실제로 배포한 이름을 따릅니다.

| 이름 | 용도 |
|---|---|
| `oh-my-opencode` | 현재 upstream npm package와 primary installer command |
| `oh-my-openagent` | 전환기 compatibility binary/product name |
| `oh-my-openagent` plugin entry | installer가 등록할 수 있을 때 선호하는 OpenCode plugin entry |
| `oh-my-opencode` plugin entry | 전환기에도 인식되는 legacy entry |

## CLI

| 명령 | 용도 |
|---|---|
| `bunx oh-my-opencode install` | 대화형 installer |
| `bunx oh-my-opencode install --no-tui ...` | agent-driven install용 비대화형 setup |
| `bunx oh-my-opencode doctor` | 시스템, config, tool, model resolution 확인 |
| `bunx oh-my-opencode run <message>` | completion enforcement가 있는 OpenCode session runner |
| `bunx oh-my-opencode version` | 버전 정보 |
| `bunx oh-my-opencode mcp oauth ...` | remote MCP server OAuth helper |

## 설정 파일

사용자 설정을 먼저 읽고, project 설정이 덮어쓸 수 있습니다. JSONC를 지원합니다.

| 범위 | 경로 |
|---|---|
| 프로젝트 | `.opencode/oh-my-openagent.json[c]`, `.opencode/oh-my-opencode.json[c]` |
| 사용자 | `~/.config/opencode/oh-my-openagent.json[c]`, `~/.config/opencode/oh-my-opencode.json[c]` |

## 빠른 예시

```jsonc
{
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-openagent/dev/assets/oh-my-opencode.schema.json",
  "agents": {
    "oracle": { "model": "openai/gpt-5.4", "variant": "high" },
    "explore": { "model": "github-copilot/grok-code-fast-1" }
  },
  "categories": {
    "quick": { "model": "opencode/gpt-5-nano" },
    "visual-engineering": { "model": "google/gemini-3.1-pro" }
  }
}
```

설치된 package가 renamed schema를 노출한다면 `doctor`가 알려주는 경로를 우선하세요.

## Agent 옵션

| 옵션 | 설명 |
|---|---|
| `model` | `provider/model` 형식의 override |
| `variant` | `max`, `high`, `medium`, `low`, `xhigh` 같은 변형 |
| `fallback_models` | model/provider 실패 시 순서대로 시도할 fallback chain |
| `prompt`, `prompt_append` | agent prompt 교체/추가. `file://` URI 지원 |
| `tools` | agent가 쓸 수 있는 도구 제한 |
| `disable` | 특정 agent 비활성화 |
| `permission` | edit, bash, webfetch 같은 권한 제어 |

## Category

| Category | 목적 |
|---|---|
| `visual-engineering` | Frontend, UI/UX, design, styling, animation |
| `ultrabrain` | hard logic, architecture, deep reasoning |
| `deep` | 자율 조사와 end-to-end 구현 |
| `artistry` | 창의적/비정형 작업 |
| `quick` | 작은 변경, typo, 단일 파일 수정 |
| `unspecified-low`, `unspecified-high` | 일반 작업의 저/고노력 fallback |
| `writing` | 문서와 prose |

## Install Flags

| Flag | 의미 |
|---|---|
| `--claude=no\|yes\|max20` | Claude provider mode |
| `--openai=no\|yes` | OpenAI/ChatGPT provider mode |
| `--gemini=no\|yes` | Gemini provider mode |
| `--copilot=no\|yes` | GitHub Copilot fallback provider |
| `--opencode-go=no\|yes` | OpenCode Go subscription path |
| `--opencode-zen=no\|yes` | `opencode/` model catalog path |
| `--zai-coding-plan=no\|yes` | Z.ai Coding Plan path |
| `--kimi-for-coding=no\|yes` | Kimi for Coding path |
| `--vercel-ai-gateway=no\|yes` | Vercel AI Gateway path |
| `--skip-auth` | 설치 후 auth 안내 생략 |

## 환경 변수

| 변수 | 의미 |
|---|---|
| `OMO_SEND_ANONYMOUS_TELEMETRY=0` | 익명 active-install telemetry 끄기 |
| `OMO_DISABLE_POSTHOG=1` | PostHog telemetry 끄기 |
| `OPENCODE_CONFIG_DIR` | OpenCode config directory override |
