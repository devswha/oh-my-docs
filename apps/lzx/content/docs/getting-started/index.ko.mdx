---
title: 시작하기
description: Codex App 또는 Codex CLI에서 LazyCodex를 설치하고, 프로젝트 메모리부터 첫 계획과 실행까지 이어갑니다.
---

이 페이지는 LazyCodex를 처음 설치한 뒤 바로 한 번 써 보는 흐름입니다. 먼저 Codex App에서 맡기는 방법과 Codex CLI에서 직접 실행하는 방법을 나눠 보고, `$init-deep`로 프로젝트 메모리를 만든 다음, `$ulw-plan`과 `$start-work`로 첫 작업을 진행합니다.

<Steps>

<Step>

### 사전 준비

- `npx`를 실행할 수 있는 Node.js/npm 환경
- Codex App 또는 [OpenAI Codex CLI](https://github.com/openai/codex)

LazyCodex는 Codex 설정에 OmO 커맨드, 스킬, 훅을 연결합니다. Codex App을 쓴다면 앱에서 작업을 맡길 수 있고, 터미널을 선호한다면 Codex CLI가 설치되어 정상 동작해야 합니다.

</Step>

<Step>

### 설치

설치 방식은 두 가지입니다. 전역 설치나 `npm i -g`는 필요 없으며, 두 경우 모두 `npx lazycodex-ai install`을 사용합니다.

<div className="not-prose my-5 grid gap-3 md:grid-cols-2">
  <section className="rounded-lg border border-fd-border bg-fd-card p-4">
    <div className="mb-3 flex items-center gap-2 font-semibold">
      <svg aria-hidden="true" className="h-4 w-4 text-fd-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        <rect width="18" height="14" x="3" y="4" rx="2" />
        <path d="M8 20h8" />
        <path d="M12 18v2" />
      </svg>
      <span>Codex App</span>
    </div>
    <p className="mb-3 text-sm text-fd-muted-foreground">
      앱에서 새 작업을 열고 Codex에게 설치를 맡깁니다. 에이전트가 명령을 실행하고 필요한 설정 파일 변경을 확인합니다.
    </p>
    <pre className="overflow-x-auto rounded-md border border-fd-border bg-fd-muted p-3 text-sm"><code>npx lazycodex-ai install</code></pre>
  </section>
  <section className="rounded-lg border border-fd-border bg-fd-card p-4">
    <div className="mb-3 flex items-center gap-2 font-semibold">
      <svg aria-hidden="true" className="h-4 w-4 text-fd-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        <path d="m7 11 2-2-2-2" />
        <path d="M11 13h4" />
        <rect width="18" height="18" x="3" y="3" rx="2" />
      </svg>
      <span>Codex CLI</span>
    </div>
    <p className="mb-3 text-sm text-fd-muted-foreground">
      터미널에서 직접 설치합니다. 설치가 끝나면 프로젝트에서 Codex를 다시 열고 `$init-deep`부터 시작합니다.
    </p>
    <pre className="overflow-x-auto rounded-md border border-fd-border bg-fd-muted p-3 text-sm"><code>npx lazycodex-ai install</code></pre>
  </section>
</div>

아래 GIF는 실제로 입력할 줄을 그대로 보여줍니다. 짧은 설치 명령과, 그 명령이 연결하는 OmO 설치 명령, 설치 후 처음 실행할 `$init-deep` 순서입니다.

<figure className="not-prose my-6">
  <img
    src="/images/docs/lazycodex-install-flow.gif"
    alt="Codex App과 Codex CLI에서 LazyCodex 설치 흐름"
    width="960"
    height="420"
    loading="lazy"
    className="w-full rounded-lg border border-fd-border bg-fd-card"
  />
</figure>

이는 Codex 플랫폼을 대상으로 하는 OmO 설치 프로그램의 단축형입니다.

```bash
npx --yes --package oh-my-openagent omo install --platform=codex
```

완전 자율, 무 TUI 세팅을 원한다면 자율 모드 플래그를 추가합니다.

```bash
npx lazycodex-ai install --no-tui --codex-autonomous
```

설치 프로그램은 OmO 하네스(커맨드, 스킬, 훅, 모델 라우팅)를 여러분의 Codex 세팅에 연결합니다. LLM 에이전트가 설치를 실행하고 세팅을 대신 진행하도록 맡기는 것을 강력히 권장합니다. 에이전트가 구독 감지, 모델 선택, 제공자 인증을 자동으로 처리합니다.

</Step>

<Step>

### 인증

별도의 LazyCodex 로그인 명령은 없습니다. 설치 프로그램(그리고 이를 대신 실행하도록 맡긴 LLM 에이전트)이 구독 감지, 모델 선택, 제공자 인증을 자동으로 처리합니다. Codex App 또는 Codex CLI 자체에는 로그인되어 있어야 하지만, 이는 사전 준비일 뿐 LazyCodex의 인증 단계는 아닙니다.

제공자와 라우팅 세부 사항은 [설정](/ko/docs/reference/configuration)을 참고하세요.

</Step>

<Step>

### 첫 작업 실행하기

먼저 프로젝트 메모리를 만든 다음, 커맨드 기둥을 사용합니다. 기둥 커맨드는 설치 프로그램이 출력하는 `$command` 구문으로 호출합니다.

먼저 에이전트가 충분한 맥락을 갖고 시작하도록 계층형 `AGENTS.md` 프로젝트 메모리를 생성합니다.

```text
$init-deep
```

그다음 작업에 맞는 기둥을 선택합니다.

- `$ulw-plan "무엇을 만들지"` — Prometheus가 접근 방식을 계획해 `plans/<slug>.md`에 작성합니다. 제품 코드는 절대 작성하지 않습니다.
- `$start-work [plan-name] [--worktree <path>]` — 모든 체크박스가 완료될 때까지 계획을 실행한 뒤 **ORCHESTRATION COMPLETE**를 출력합니다.
- `$ulw-loop "작업" [--completion-promise=TEXT] [--strategy=reset|continue]` — Oracle이 검증한 완료에 이를 때까지 실행되는 자기 참조 루프입니다.

일반적인 흐름은 먼저 계획하고 실행합니다.

```text
$init-deep
$ulw-plan "작업 관리를 위한 REST API를 만든다"
$start-work
```

또는 결과가 증거로 검증될 때까지 계속 진행해야 하는 열린 작업이라면 루프를 바로 사용합니다.

```text
$ulw-loop "결제 흐름의 실패를 수정하고 처음부터 끝까지 검증한다"
```

</Step>

</Steps>

## 스킬

커맨드 기둥 외에도 LazyCodex는 전문 작업을 위한 OmO 스킬 레이어(`review-work`, `remove-ai-slops`, `frontend-ui-ux`, `programming`, `LSP`, `rules`, `comment-checker` 등)를 설치합니다. 커맨드 레이어는 단순하게 유지되고, 스킬이 전문가의 판단을 더합니다. 전체 라이브러리는 [스킬](/ko/docs/skills)을 참고하세요.

## 다음 단계

<Cards>
  <Card title="커맨드" href="/ko/docs/commands" description="커맨드 기둥: ulw-plan, start-work, ulw-loop, init-deep." />
  <Card title="개념" href="/ko/docs/concepts" description="ultrawork 루프, 규율 에이전트, 모델 라우팅." />
  <Card title="사용 사례 & 가이드" href="/ko/docs/guides" description="일상 작업을 위한 실제 예시." />
</Cards>

- 기반 엔진: [oh-my-openagent (OmO)](https://github.com/code-yeongyu/oh-my-openagent)
- [GitHub의 LazyCodex](https://github.com/code-yeongyu/lazycodex)
